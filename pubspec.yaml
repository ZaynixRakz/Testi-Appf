name: zaynix_forsaken
description: "A new Flutter project for Zaynix ForSaken Utility."
publish_to: 'none'
version: 3.0.3+1

environment:
  sdk: '>=2.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
